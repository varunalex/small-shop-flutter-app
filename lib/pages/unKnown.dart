import 'package:flutter/material.dart';

class UnKnownPage extends StatelessWidget {
  @override
    Widget build(BuildContext context) {
      return Scaffold(
        appBar: AppBar(
          title: Text('Un-Known Page'),
        ),
        body: Center(
          child: Text('Unkonwn Page'),
        ),
      );
    }
}